# Worker Configuration
# Worker URL - địa chỉ của Worker service
# Có thể override bằng environment variable WORKER_URL
worker.url=http://localhost:8081

# Nếu Worker không khả dụng, hệ thống sẽ tự động fallback về local conversion

